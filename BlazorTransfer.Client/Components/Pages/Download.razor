@page "/download/{Id}"
@inject NavigationManager Nav

@code {
    [Parameter] public string Id { get; set; } = default!;
    protected override void OnInitialized()
    {
        Nav.NavigateTo($"api/transfer/download/{Id}", forceLoad: true);
    }
}